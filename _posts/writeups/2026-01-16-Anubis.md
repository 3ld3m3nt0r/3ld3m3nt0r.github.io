---
layout: post
title: HackTheBox-Anubis
description: Resolucion de la maquina Anubis de HackTheBox
date: 2026-01-10 20:24:59 -500
categories: [Active Directory, Insane, Chisel, XSS]
tags: [Ciberseguridad, Windows, Insane]
home_image: logo
logo:
  path: /assets/writeups/Anubis.png
  alt: "RE"
---
## ESCALADA 

### /etc/krb5.conf

```shell
[libdefaults]
        default_realm = WINDCORP.HTB

[realms]
        WINDCORP.HTB = {
                kdc = EARTH.WINDCORP.HTB
                admin_server = EARTH.WINDCORP.HTB
                pkinit_anchors = FILE:/opt/krb/ca.cer
                pkinit_identities = FILE:/opt/krb/admin.cer,/opt/krb/admin.key
                pkinit_kdc_hostname = EARTH.WINDCORP.HTB
                pkinit_eku_checking = kpServerAuth
        }

[domain_realm]
        .windcorp.htb = WINDCORP.HTB
```

```shell
┌──────────────┐
│ Usuario AD   │
│ DiegoCruz    │
│ (webdevelopers)
└──────┬───────┘
       │
       │ Miembro de grupo con Full Control
       ▼
┌──────────────────────────────┐
│ Plantilla AD CS: "Web"       │
│ - ENROLLEE_SUPPLIES_SUBJECT  │
│ - Full Control: webdevelopers│
│ - EKU: Server Auth           │
└──────┬───────────────────────┘
       │
       │ Modificación de plantilla
       ▼
┌──────────────────────────────┐
│ Plantilla "Web" modificada   │
│ - Client Authentication      │
│ - Smart Card Logon (PKINIT)  │
│ - SAN controlado (UPN admin) │
└──────┬───────────────────────┘
       │
       │ Solicitud CSR
       ▼
┌──────────────────────────────┐
│ CSR OpenSSL                  │
│ CN=Administrator             │
│ SAN=administrator@domain     │
└──────┬───────────────────────┘
       │
       │ certreq -submit
       ▼
┌──────────────────────────────┐
│ CA windcorp-CA               │
│ Firma certificado admin      │
│ (porque confía en plantilla) │
└──────┬───────────────────────┘
       │
       │ admin.cer + admin.key
       ▼
┌──────────────────────────────┐
│ PKINIT Kerberos              │
│ kinit con certificado        │
│ (Smart Card Logon)           │
└──────┬───────────────────────┘
       │
       │ Clock skew (resuelto)
       ▼
┌──────────────────────────────┐
│ faketime + proxychains       │
│ Ticket TGT Administrator     │
└──────┬───────────────────────┘
       │
       │ Kerberos auth
       ▼
┌──────────────────────────────┐
│ Evil-WinRM (Kerberos)        │
│ Shell como Administrator     │
└──────────────────────────────┘
```